image: informaticsmatters/rdkit:latest

before_script:
  - python --version
  - pip install -r requirements.txt

stages:
  - test

unittest:
  stage: test
  script:
    - pwd
    - ls -l
    - export PYTHONPATH="$PYTHONPATH:."
    - python -c "import sys;print(sys.path)"
    - pytest
